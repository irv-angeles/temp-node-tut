const num1 = 5;
const num2 = 10;

function addValues() {
  console.log(`the sum is : ${num1 + num2}`);
}

// if we call the function it will display the result if we call require("./filename") in our app.js
//if you execute it
addValues();
